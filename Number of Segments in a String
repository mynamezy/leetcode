class Solution {
public:
    int countSegments(string s) {
        int count=0;
        for(auto c:s){
            if(ispunct(c)||isspace(c))
            count++;
        }
        return count;
    }
};
