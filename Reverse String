class Solution {
public:
    string reverseString(string s) {
        string s1="";
        int count=0;
        for(auto w:s) count++;
        for(auto i=count-1;i>=0;i--){
            s1+=s[i];
        }
        return s1;
     }
};
